# ASR

### Pre-reqs
- .net 9.0 SDK installed
- patience

### Getting
```bash
git clone --recursive https://github.com/rickz0rz/ASR
````

### Running Tests
```bash
cd m68000
python3 decode.py
cd ..
dotnet test
````

Lots of tests currently fail.

### Running the Application (just now is "hello world")
```bash
dotnet run -p ASR
````